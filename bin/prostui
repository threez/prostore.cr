#!/usr/bin/env crystal
require "../src/prostore"
require "../src/prostore/tui/app"

url = ENV["DATABASE_URL"]? || (STDERR.puts("DATABASE_URL not set"); exit 1)

conn = Prostore::Connection.open(url)
begin
  Prostore::TUI::App.run(conn)
ensure
  conn.close
end
